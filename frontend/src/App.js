import React from "react";

import "./global.css";

import Routes from "./routes";

function App() {
  return <Routes />;
}

export default App;

/**
 * props, passados junto com a tag ou componente <tag title="Hello" />
 * props.children, são os elementos dentro da tag <tag> Tudo aqui é filho </tag>
 */
